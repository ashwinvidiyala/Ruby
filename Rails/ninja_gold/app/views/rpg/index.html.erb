<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Ninja Gold Django Style</title>
  </head>
  <body>
    <h3>Your Gold: <%= session[:gold] %></h3>
    <div>
      <form action="<%= rpg_path 'farm' %>" method="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <h2>Farm</h2>
        <p>(earns 10-20 golds)</p>
        <!-- <input type="hidden" name="building" value="farm" /> -->
        <input type="submit" value="Find Gold!"/>
      </form>
    </div>
    <div>
      <form action="<%= rpg_path 'cave' %>" method="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <h2>Cave</h2>
        <p>(earns 5-10 golds)</p>
        <!-- <input type="hidden" name="building" value="cave" /> -->
        <input type="submit" value="Find Gold!"/>
      </form>
    </div>
    <div>
      <form action="<%= rpg_path 'house' %>" method="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <h2>House</h2>
        <p>(earns 2-5 golds)</p>
        <!-- <input type="hidden" name="building" value="house" /> -->
        <input type="submit" value="Find Gold!"/>
      </form>
    </div>
    <div>
      <form action="<%= rpg_path 'casino' %>" method="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <h2>Casino</h2>
        <p>(earns/takes 0-50 golds)</p>
        <!-- <input type="hidden" name="building" value="casino" /> -->
        <input type="submit" value="Find Gold!"/>
      </form>
    </div>

    <h2>Activities:</h2>
    <% if session[:activity] %>
      <% session[:activity].each do |activity| %>
      <p> <%= activity %></p>
      <% end %>
    <% end %>
  </body>
</html>
